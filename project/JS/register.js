document.addEventListener('DOMContentLoaded', function () {

    var submitButton = document.querySelector('.reg_body .sub');

    submitButton.addEventListener('click', () => {
        window.location.href = './index.js'
    })


})
